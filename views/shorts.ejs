<%include ./partials/header%>
<div class="right h-[85vh] flex-1 relative px-6 overflow-y-auto">
    <div class="container mx-auto  p-4 relative overflow-hidden">
        <div class="content flex flex-col gap-10 transition-transform duration-500">
            <div class="short-container flex justify-center overflow-hidden relative">
                <div class="short-panel w-[20rem] h-[80vh] bg-zinc-200 rounded-lg overflow-hidden">
                    <!-- Slide content goes here -->
                </div>
                <div class="options flex flex-col px-3">
                    <!-- Options content goes here -->
                </div>
            </div>
            <!-- Add more slides -->
            <div class="short-container flex justify-center overflow-hidden relative">
                <div class="short-panel w-[20rem] h-[80vh] bg-zinc-200 rounded-lg overflow-hidden">
                    <!-- Slide content goes here -->
                </div>
                <div class="options flex flex-col px-3">
                    <!-- Options content goes here -->
                </div>
            </div>
            <!-- Add more slides -->
        </div>


</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js" integrity="sha512-7eHRwcbYkK4d9g/6tD/mhkf++eoTHwpNM9woBxtPUBWm67zeAfFC+HrdoE2GanKeocly/VxeLvIqwvCdk7qScg==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
<script>
     document.addEventListener('DOMContentLoaded', function () {
        const container = document.querySelector('.container');
        const content = document.querySelector('.content');
        const items = document.querySelectorAll('.short-container');

        let currentIndex = 0;

        container.addEventListener('wheel', function (event) {
            event.preventDefault();
            
            const delta = Math.sign(event.deltaY);

            if (delta > 0 && currentIndex < items.length - 1) {
                currentIndex++;
            } else if (delta < 0 && currentIndex > 0) {
                currentIndex--;
            }

            const itemHeight = items[0].offsetHeight;
            const newPosition = currentIndex * -itemHeight;

            content.style.transform = `translateY(${newPosition}px)`;
        });
    });
</script>
<%include ./partials/footer%>